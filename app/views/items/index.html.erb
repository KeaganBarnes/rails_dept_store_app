<h1>View Items</h1>
<%= link_to 'List a New Item', new_department_item_path(@department) %>
<ul>
  <% @items.each do |item| %>
    <li><%= link_to item.name, [@department, @item] %></li>
  <% end %>
</ul>